- name: Creating AMI
  ec2_ami:
    aws_access_key: "{{ ec2_access_key }}"
    aws_secret_key: "{{ ec2_secret_key }}"
    region: "{{ us-east-2 }}"
    description: "{{ ram }}"
    instance_id: "{{ i-085df31cfa8465e46 }}"
    wait: yes
    name: "{{ ami }}"
    state: present
