---
- name: creating ec2_ami
  vars:
    ec2_instance_id: i-054fcfa2e530a558a
    region: us-east-2
  tasks:
  ec2_ami:
    instance_id: "{{ ec2_instance_id }}"
    region: "{{ region }}"
    wait: yes
    name: newtest
    tags:
      Name: newtest
      Service: TestService
